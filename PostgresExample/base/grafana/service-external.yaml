apiVersion: v1
kind: Service

metadata:
  name: grafana-external

spec:
  selector:
    app: grafana
  type: NodePort
  ports:
  - name: ui
    port: 3000
    targetPort: http-grafana
    nodePort: 31000
    protocol: TCP
